[phases.setup]
nixPkgs = ['python311', 'postgresql']
nixLibs = ['python311Packages.pip', 'python311Packages.setuptools']

[phases.install]
cmds = ['python -m pip install --upgrade pip', 'python -m pip install -r requirements.txt']

[phases.build]
cmds = ['python manage.py collectstatic --noinput']

[start]
cmd = 'gunicorn waste_management.wsgi:application --bind 0.0.0.0:$PORT'